<?php

/*
  * implementation our hook 
  */

function anzy_form_alter(&$form, &$form_state, $form_id) {
  // altering search to make it invisible for search icon
  if ($form_id == 'search_block_form') {
    $form['actions']['submit']['#value'] = t(''); // Change the text on the submit button
  }
}
